<Page x:Class="LoadMate.Pages.ClientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LoadMate.Pages"
      mc:Ignorable="d" 
   Title="Клиент" 
      Height="768" Width="1366"
      Background="{StaticResource BackgroundLight}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SidebarBg}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,30,0,0">
                <TextBlock Text="LoadMate" FontSize="28" FontWeight="Bold" 
                           Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <Border Background="{StaticResource PrimaryLightColor}" CornerRadius="8" Margin="20,0,20,20" Padding="15">
                    <StackPanel>
                        <TextBlock Text="{Binding FullName}" FontSize="16" FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Клиент" FontSize="12" 
                                   Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Button x:Name="btnCreateOrder" Content="📝 Создать заказ" Style="{StaticResource SidebarButton}" Click="CreateOrder_Click" Margin="10,5"/>
                <Button x:Name="btnMyOrders" Content="📋 Мои заказы" Style="{StaticResource SidebarButton}" Click="MyOrders_Click" Margin="10,5"/>
                <Button x:Name="btnPayments" Content="💳 Платежи" Style="{StaticResource SidebarButton}" Click="Payments_Click" Margin="10,5"/>

                <Button Content="🚪 Выход" Style="{StaticResource SidebarButton}" Click="Logout_Click" Margin="10,5" Foreground="{StaticResource ErrorColor}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="30,30,30,30">
            <Frame x:Name="ClientFrame" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Page>