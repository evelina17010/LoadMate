<Page x:Class="LoadMate.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LoadMate.Pages"
      mc:Ignorable="d" 
 Title="Администратор" 
      d:DesignHeight="768" d:DesignWidth="1366"
      Background="{StaticResource BackgroundLight}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Border Background="{StaticResource PrimaryColor}" 
                    CornerRadius="0,40,0,0" 
                    Margin="0,70,0,0" 
                    VerticalAlignment="Stretch">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="120"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" VerticalAlignment="Top" Margin="0,45,0,0">
                        <TextBlock Text="LoadMate" Foreground="White" FontSize="20" FontWeight="Bold" 
               HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding FullName}" 
               Foreground="White" FontSize="12" 
               HorizontalAlignment="Center" Opacity="0.7" Margin="0,5,0,0"
               TextWrapping="Wrap" TextAlignment="Center"/>

                        <Separator Background="White" Opacity="0.3" Margin="40,20,40,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="20,30,20,0">
                        <Button Content="Пользователи" Style="{StaticResource SidebarButton}" Click="Users_Click"/>
                        <Button Content="Заказы" Style="{StaticResource SidebarButton}" Click="Orders_Click"/>
                        <Button Content="Грузы" Style="{StaticResource SidebarButton}" Click="Cargo_Click"/>
                        <Button Content="Водители" Style="{StaticResource SidebarButton}" Click="Drivers_Click"/>
                        <Button Content="Транспорт" Style="{StaticResource SidebarButton}" Click="Trucks_Click"/>
                    </StackPanel>

                    <Button Grid.Row="2" Content="Выход" Style="{StaticResource SidebarButton}" 
        Click="Logout_Click" Margin="20,0,20,40"/>
                </Grid>
            </Border>

            <Border Width="90" Height="90" 
                    Background="White" 
                    CornerRadius="45" 
                    Padding="4"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Top" 
                    Margin="0,25,0,0"
                    Panel.ZIndex="2"
                    Effect="{StaticResource CardShadow}">
                <Border CornerRadius="45">
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding UserPhoto}" Stretch="UniformToFill"/>
                    </Border.Background>
                </Border>
            </Border>
        </Grid>

        <Grid Grid.Column="1" Background="Transparent">
            <Frame x:Name="AdminFrame" NavigationUIVisibility="Hidden" Margin="20"/>
        </Grid>
    </Grid>
</Page>