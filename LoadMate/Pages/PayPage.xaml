<Page x:Class="LoadMate.Pages.PayPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LoadMate.Pages"
      mc:Ignorable="d" 
     d:DesignHeight="750" d:DesignWidth="500"
       Title="Оплата заказа"
      Background="{StaticResource BackgroundLight}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Border Style="{StaticResource Card}" Padding="25" Margin="40" MaxWidth="450">
            <StackPanel>
                <Button Content="← Назад к платежам" HorizontalAlignment="Left" 
                        Style="{StaticResource OutlineButton}" Background="Transparent" 
                        Foreground="{StaticResource TextSecondary}" Click="Cancel_Click" Margin="0,0,0,15" FontSize="14"/>

                <TextBlock Text="Оплата заказа" FontSize="24" FontWeight="SemiBold" 
                           Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <Border Background="#F8FAFC" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <Grid Margin="0,2">
                            <TextBlock Text="Номер заказа:" Foreground="{StaticResource TextSecondary}"/>
                            <TextBlock x:Name="txtOrderNumber" HorizontalAlignment="Right" FontWeight="Medium"/>
                        </Grid>
                        <Grid Margin="0,2">
                            <TextBlock Text="К оплате:" Foreground="{StaticResource TextSecondary}"/>
                            <TextBlock x:Name="txtAmount" HorizontalAlignment="Right" FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryColor}" FontSize="18"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <TextBlock Text="Способ оплаты" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="cmbPaymentMethod" Style="{StaticResource ComboBoxField}" Height="40" Margin="0,5,0,20">
                    <ComboBoxItem Content="Банковская карта" IsSelected="True"/>
                    <ComboBoxItem Content="СБП (Система быстрых платежей)"/>
                    <ComboBoxItem Content="Электронный кошелек"/>
                </ComboBox>

                <TextBlock Text="Номер карты" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="txtCardNumber" Style="{StaticResource InputField}" Height="40" Margin="0,5,0,15" MaxLength="19" TextChanged="txtCardNumber_TextChanged"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Срок (ММ/ГГ)" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="txtExpiry" Style="{StaticResource InputField}" Height="40" MaxLength="5" TextChanged="txtExpiry_TextChanged"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="CVV" Style="{StaticResource FieldLabel}"/>
                        <PasswordBox x:Name="txtCvv" Style="{StaticResource PasswordField}" Height="40" MaxLength="3"/>
                    </StackPanel>
                </Grid>

                <CheckBox x:Name="chkSaveCard" Content="Сохранить данные карты" Margin="0,10,0,25"/>

                <Button Content="Подтвердить и оплатить" Style="{StaticResource ActionButton}" 
                        Height="45" Click="Pay_Click" FontSize="16"/>
            </StackPanel>
        </Border>
    </ScrollViewer>
</Page>