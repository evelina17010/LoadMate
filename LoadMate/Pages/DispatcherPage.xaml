<Page x:Class="LoadMate.Pages.DispatcherPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LoadMate.Pages"
      mc:Ignorable="d" 
      Title="Панель диспетчера" 
          d:DesignHeight="768" d:DesignWidth="1366"
      Background="{StaticResource BackgroundLight}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource BackgroundWhite}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="LoadMate" FontSize="24" FontWeight="Bold" 
                           Foreground="{StaticResource PrimaryColor}" Margin="0,40,0,30" HorizontalAlignment="Center"/>

                <Border Grid.Row="1" Background="{StaticResource BackgroundLight}" CornerRadius="12" Margin="20,0,20,30" Padding="15">
                    <StackPanel>
                        <Grid HorizontalAlignment="Center" Margin="0,0,0,10">
                            <Ellipse Width="45" Height="45" Fill="{StaticResource PrimaryColor}"/>
                            <TextBlock Text="Д" Foreground="White" VerticalAlignment="Center" 
                                       HorizontalAlignment="Center" FontWeight="Bold" FontSize="18"/>
                        </Grid>
                        <TextBlock Text="{Binding FullName}" FontSize="15" FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center" TextAlignment="Center"/>
                        <TextBlock Text="Диспетчер" FontSize="11" Foreground="{StaticResource TextSecondary}" 
                                   HorizontalAlignment="Center" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <StackPanel Grid.Row="2">
                    <Button x:Name="btnOrders" Content=" Все заказы" Style="{StaticResource SidebarButton}" Click="Orders_Click" Margin="10,2"/>
                    <Button x:Name="btnCreateOrder" Content=" Новый заказ" Style="{StaticResource SidebarButton}" Click="CreateOrder_Click" Margin="10,2"/>
                    <Border Height="1" Background="{StaticResource BorderColor}" Margin="25,10" Opacity="0.5"/>
                    <Button x:Name="btnDrivers" Content=" Водители" Style="{StaticResource SidebarButton}" Click="Drivers_Click" Margin="10,2"/>
                    <Button x:Name="btnTrucks" Content=" Автопарк" Style="{StaticResource SidebarButton}" Click="Trucks_Click" Margin="10,2"/>
                    <Button x:Name="btnTariffs" Content=" Управление тарифами" Style="{StaticResource SidebarButton}" Click="Tariffs_Click" Margin="10,2"/>
                </StackPanel>
                <Button Grid.Row="3" Content=" Выход" Style="{StaticResource SidebarButton}" 
                        Click="Logout_Click" Margin="10,0,10,25" Foreground="{StaticResource ErrorColor}"/>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="30">
            <Frame x:Name="DispatcherFrame" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Page>