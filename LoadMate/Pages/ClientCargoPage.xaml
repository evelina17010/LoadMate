<Page x:Class="LoadMate.Pages.ClientCargoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LoadMate.Pages"
      mc:Ignorable="d" 
  Title="Мои грузы" 
      d:DesignHeight="768" d:DesignWidth="1366"
      Background="{StaticResource BackgroundLight}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Обновить" Style="{StaticResource ActionButton}" Click="Refresh_Click" Width="120" Height="40"/>
            <TextBox x:Name="txtSearch" Style="{StaticResource InputField}" Width="250" Height="40" Margin="20,0,0,0" 
                     VerticalContentAlignment="Center" TextChanged="Search_TextChanged" Tag="Поиск груза..."/>
        </StackPanel>

        <DataGrid x:Name="CargoGrid" Grid.Row="1" Style="{StaticResource DataGridStyle}" 
                  AutoGenerateColumns="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Cargo_id}" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Тип груза" Binding="{Binding CargoTypeName}" Width="150"/>
                <DataGridTextColumn Header="Вес (кг)" Binding="{Binding Weight_kg}" Width="100"/>
                <DataGridTextColumn Header="Объем (м³)" Binding="{Binding Volume_m3}" Width="100"/>
                <DataGridTextColumn Header="Хрупкий" Binding="{Binding Is_fragile}" Width="100"/>
                <DataGridTextColumn Header="Опасный" Binding="{Binding Is_dangerous}" Width="100"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding Created_at, StringFormat=dd.MM.yyyy}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>