<Page x:Class="LoadMate.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
  Title="Главная"
      d:DesignHeight="850" d:DesignWidth="1366"
      Background="{StaticResource BackgroundLight}">

    <ScrollViewer VerticalScrollBarVisibility="Hidden">
        <StackPanel>
            <Grid Height="600">
                <Border Background="{StaticResource PrimaryGradient}" CornerRadius="0,0,80,80">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" Opacity="0.15" ShadowDepth="10"/>
                    </Border.Effect>
                </Border>

                <Grid Margin="60,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="/Resources/iconslog.png" Height="40" Width="40" Margin="0,0,10,0"/>
                            <TextBlock Text="LoadMate" FontSize="30" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0 0 10 0">
                            <Button Content="Вход" Style="{StaticResource OutlineButton}" FontSize="20"  Foreground="White"  Width="100" Height="40"  Click="Login_Click"/>
                            <Button Content="Регистрация" Style="{StaticResource OutlineButton}" Background="Transparent" Foreground="White" Width="140" Height="40" FontSize="20" Click="Register_Click"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,50">
                     
                        <TextBlock Text="LoadMate" FontSize="86" FontWeight="ExtraBold" Foreground="White" HorizontalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="2"/>
                            </TextBlock.Effect>
                        </TextBlock>

                        <TextBlock Text="Умное управление перевозками в один клик." FontSize="24" Foreground="White" HorizontalAlignment="Center" Opacity="0.9" Margin="0,10,0,40"/>

                        <Button Content="Начать работу"   Style="{StaticResource ActionButton}" Background="#44FFFFFF"  Foreground="White"  BorderBrush="White"
        BorderThickness="1.5" Width="220"  Height="55"  FontSize="18"  FontWeight="SemiBold" Click="StartWork_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="27"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>

            <Grid Margin="60,-80,60,60">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource Card}" Margin="15" Padding="30">
                    <StackPanel>
                        <Border Background="{StaticResource PrimaryLightColor}" Width="60" Height="60" CornerRadius="15" HorizontalAlignment="Left" Margin="0,0,0,20">
                            <TextBlock Text="🚚" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Грузоперевозки" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Доставка любой сложности по всей территории России с полным контролем." FontSize="14" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" LineHeight="20"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource Card}" Margin="15" Padding="30">
                    <StackPanel>
                        <Border Background="#FFFEEBC8" Width="60" Height="60" CornerRadius="15" HorizontalAlignment="Left" Margin="0,0,0,20">
                            <TextBlock Text="⚡" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Прямая доставка" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Экспресс-доставка от двери до двери без лишних задержек и складов." FontSize="14" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" LineHeight="20"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource Card}" Margin="15" Padding="30">
                    <StackPanel>
                        <Border Background="#EBF4FF" Width="60" Height="60" CornerRadius="15" HorizontalAlignment="Left" Margin="0,0,0,20">
                            <TextBlock Text="📊" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Мониторинг" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Умная система отслеживания заказов и аналитика в реальном времени." FontSize="14" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" LineHeight="20"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Background="{StaticResource BackgroundWhite}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0" Padding="40">
                <Grid>
                    <TextBlock Text="© 2026 LoadMate Team" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="Поддержка" Margin="20,0" Foreground="{StaticResource TextSecondary}" Cursor="Hand"/>
                        <TextBlock Text="Политика конфиденциальности" Foreground="{StaticResource TextSecondary}" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>