<Page x:Class="LoadMate.Pages.ClientCreateOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LoadMate.Pages"
      mc:Ignorable="d" 
   Title="Создание заказа" 
            d:DesignHeight="768" d:DesignWidth="1366"
      Background="{StaticResource BackgroundLight}">

    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="650">
            <Border Style="{StaticResource Card}" Padding="40">
                <StackPanel>
                    <TextBlock Text="Создание заказа" FontSize="28" FontWeight="SemiBold" 
                               Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                    <TextBlock Text="Описание груза" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtDescription" Style="{StaticResource InputField}" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

                    <TextBlock Text="Тип груза" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="cmbCargoType" Style="{StaticResource ComboBoxField}" Height="45"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Вес (кг)" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="txtWeight" Style="{StaticResource InputField}" Height="45"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Объем (м³)" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="txtVolume" Style="{StaticResource InputField}" Height="45"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Откуда" Style="{StaticResource FieldLabel}" Margin="0,15,0,5"/>
                    <TextBox x:Name="txtPickupAddress" Style="{StaticResource InputField}" Height="45" ToolTip="Город, улица, дом"/>

                    <TextBlock Text="Куда" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="txtDeliveryAddress" Style="{StaticResource InputField}" Height="45" ToolTip="Город, улица, дом"/>

                    <TextBlock Text="Желаемая дата отправки" Style="{StaticResource FieldLabel}"/>
                    <DatePicker x:Name="dpScheduledPickup" Style="{StaticResource DatePickerField}" Height="45"/>

                    <TextBlock Text="Тариф" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="cmbTariff" Style="{StaticResource ComboBoxField}" Height="45" SelectionChanged="Tariff_SelectionChanged"/>

                    <Border Background="{StaticResource BackgroundLight}" CornerRadius="8" Padding="15" Margin="0,15,0,0">
                        <TextBlock x:Name="txtCost" Text="Стоимость: 0 руб." FontSize="16" FontWeight="Bold" 
                                   Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center"/>
                    </Border>

                    <Button Content="Рассчитать стоимость" Style="{StaticResource ActionButton}" Height="45" Click="CalculateCost_Click" Margin="0,15,0,10"/>
                    <Button Content="Оформить заказ" Style="{StaticResource ActionButton}" Background="{StaticResource PrimaryColor}" Height="50" FontSize="16" Click="CreateOrder_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>