<Window x:Class="LoadMate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoadMate"
        mc:Ignorable="d"
        Title="LoadMate - Логистическая система" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="#F0F9F0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryColor}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <TextBlock Text="LoadMate - Логистическая система" Foreground="White"  VerticalAlignment="Center"   Margin="10,0,0,0" FontSize="14"/>
                <Button Content="✕"  HorizontalAlignment="Right"  VerticalAlignment="Center" Width="35" Height="35" Background="Transparent" Foreground="White"
                   BorderThickness="0" FontSize="16" Click="CloseButton_Click"  Cursor="Hand"/>
            </Grid>
        </Border>
        <Frame Grid.Row="1" Name="frame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>