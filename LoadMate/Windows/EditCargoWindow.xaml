<Window x:Class="LoadMate.Windows.EditCargoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoadMate.Windows"
        mc:Ignorable="d"
 Title="Редактирование груза" 
        Height="600" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundLight}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Border Style="{StaticResource Card}" Padding="20" Margin="15">
            <StackPanel>
                <TextBlock Text="Редактирование данных груза" FontSize="20" FontWeight="Bold" 
                           Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Владелец (Клиент)" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="cmbClient" Style="{StaticResource ComboBoxField}" Height="35" SelectedValuePath="User_id">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Full_name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="Категория груза" Style="{StaticResource FieldLabel}" Margin="0,10,0,0"/>
                <ComboBox x:Name="cmbCargoType" Style="{StaticResource ComboBoxField}" Height="35" SelectedValuePath="CargoType_id">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="Описание" Style="{StaticResource FieldLabel}" Margin="0,10,0,0"/>
                <TextBox x:Name="txtDescription" Style="{StaticResource InputField}" Height="60" TextWrapping="Wrap"/>

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Вес (кг)" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="txtWeight" Style="{StaticResource InputField}" Height="35"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Объем (м³)" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="txtVolume" Style="{StaticResource InputField}" Height="35"/>
                    </StackPanel>
                </Grid>

                <StackPanel Margin="0,15,0,0">
                    <CheckBox x:Name="chkFragile" Content="Хрупкое содержимое" Margin="0,0,0,5"/>
                    <CheckBox x:Name="chkDangerous" Content="Опасный груз"/>
                </StackPanel>

                <UniformGrid Columns="2" Margin="0,25,0,0">
                    <Button Content="Сохранить" Style="{StaticResource ActionButton}" Margin="5" Click="Save_Click"/>
                    <Button Content="Отмена" Style="{StaticResource ActionButton}" Background="{StaticResource TextSecondary}" Margin="5" Click="Cancel_Click"/>
                </UniformGrid>
            </StackPanel>
        </Border>
    </ScrollViewer>
</Window>